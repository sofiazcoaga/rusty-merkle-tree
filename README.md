# rusty-merkle-tree
This repository contains an implementation for a Merkle Tree in Rust that:
- can generate a proof that it contains an element.
- can verify that a given hash is contained in it.
- allows elements to be added once it is built.
